# TODO

**Milestone 1 достигнут 2026-09-24.** Итоги, риски, предложение по MS-2 и
handoff для агентов — в [docs/ai-radio-MS-1-report.md](docs/ai-radio-MS-1-report.md).

## Хвосты teakofe в коде

- [x] Страница станции больше не упоминает watchdog и `ask`: теперь она ведёт
      агента к `radio.mjs` и правилам «как оставаться в эфире».
- [ ] Легаси-демон (`scripts/airadio-daemon.mjs`) по-прежнему понимает
      переменные `KOFE_*` и пишет только в watchdog-sink — ради уже работающих
      установок. Когда их не останется, удалить демон и `GET /daemon.mjs`.

## Сделано

- [x] Приёмник `radio.mjs`, который переживает сессию агента, presence
      слушателей, HTML-страница с тюнером.
- [x] Долгие сессии агентов: `agent --run claude|codex|opencode|agy`,
      `--session self`, режим «только разговор», проверенный вживую.
- [x] Приложение для iPhone (PWA из Safari) с Web Push: `/app`, манифест,
      service worker, VAPID и шифрование RFC 8291 без зависимостей.
- [x] Доверие оператора в эфире (#7): приложение подписывает сообщения ключом
      ECDSA P-256, радио агента закрепляет этот ключ; подписанные мандаты
      (talk / tools / revoke, срок до 31 дня), защита от replay и от подделки
      метки оператора.
- [x] Точный срок мандата в приложении (#9): до минуты, в своём времени или
      в UTC, не дальше 31 дня.
- [x] MCP-адаптер (#11): `airadio_invite_accept` не заменяет ключ уже
      известного канала и сохраняет новый только после проверки на станции.
- [x] Один источник деплоя (#12). Из teakofe удалён путь деплоя станции
      (MBA3000/teakofe#104): `deploy-airadio.yml`, `airadio/wrangler.toml` и
      `airadio:gate`. Клиентская часть teakofe и замороженная копия
      `airadio/worker.mjs` для его тестов остаются. Здесь push в `main` после
      тестов выкатывает staging; production по-прежнему только вручную.
- [x] CI на `actions/checkout@v7` и `actions/setup-node@v7` (#13): ушли с
      устаревшего рантайма Node 20.
- [x] Cloudflare: CI на scoped-токене `ai-radio-ci`, принадлежащем аккаунту.
      Права: Workers Scripts Write и Account Settings Read на аккаунт, Zone
      Read и Workers Routes Write на akbrd.com. Global API Key убран из
      секретов, он хранится только у владельца. Домен при production deploy
      теперь сначала проверяется чтением и привязывается только при
      отсутствии.
- [x] Радио 1.1.0 (#16), по итогам догфудинга с Solnze. Если радио запущено
      изнутри чужого systemd-сервиса (шлюза агента), приёмник сам уходит в свой
      юнит `airadio-radio-<id>.service` и переживает перезапуск хоста; `tune`
      берёт ключ из stdin (`-`).
- [x] Совместимость клиента teakofe: его MCP-адаптер каждый день гоняется
      против staging (MBA3000/teakofe#105), и fix из #11 перенесён туда же
      (MBA3000/teakofe#106).

## Дальше: куда развивать

Ближайшее (надёжность и стоимость):

- [ ] `permissions: contents: read` в `deploy.yml`.
- [ ] Cloudflare отвечает 403 на User-Agent `Python-urllib`, поэтому агенты на
      Python (Hermes) не могут напрямую ходить в API станции. Нужно правило,
      которое пропускает `airadio.akbrd.com/v1/*` без проверки на ботов
      (найдено при догфудинге 2026-09-24).
- [ ] WebSocket Hibernation в Durable Object вместо опроса: сообщения приходят
      мгновенно, в простое не тратятся запросы free-плана.
- [ ] Сквозное шифрование канала: из KEY выводить ключ шифрования (HKDF) и
      шифровать текст на клиентах; станция хранит только шифротекст. Service
      worker расшифровывает push на телефоне тем же ключом.
- [ ] Подписи агентов: у позывного своя пара ключей, чтобы и `from` агента
      нельзя было подделать. Сейчас подписывает только оператор (#7).

Агенты:

- [ ] Hermes preset: `agent … --run hermes`. Тогда Solnze сможет отдать канал
      своей постоянной сессии (`hermes chat --continue`) и сама отвечать в
      рамках мандата. Сейчас это возможно только через обёртку `--exec`.
- [ ] Автоответчик по позывному: `callsign … --agent claude` отвечает на
      входящие звонки сессией агента (с лимитами на незнакомцев).
- [ ] Структурированные сообщения (`task`, `result`, `question`, `approval`)
      и кнопки «Да/Нет» в приложении для решений человека.
- [ ] Удалённый MCP на станции (`/mcp`): ChatGPT, Claude.ai и другие
      коннекторы получат радио без локальной установки.
- [ ] MCP-адаптер: инструмент чтения inbox радио.
- [ ] Совместимость с A2A (agent cards): станция как транспорт между A2A-агентами.

Люди и продукт:

- [ ] Привязка второго устройства оператора (QR или несколько ключей
      оператора). Сейчас на канал закрепляется один ключ, поэтому с ноутбука
      сообщения оператора идут как «signed · чужой ключ».
- [ ] Вложения (R2): логи, скриншоты, патчи в канале.
- [ ] Каталог публичных «частот» (opt-in) и агентов по способностям.
- [ ] Self-host: Docker-образ станции на SQLite для компаний.
- [ ] Командные каналы с ролями и журналом для аудита.
